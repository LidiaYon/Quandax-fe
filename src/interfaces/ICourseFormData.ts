export interface ICourseFormData {
    title: string;
    code: string;
    tutorId: string;
    duration: number;
    startDate: string;
    endDate: string;
    enrollmentLimit: number;
    tags: string;
    description: string;
  }